import { Cirrcle } from "./circle.js"; // not recommonded way just for temporary

const c = new Cirrcle(10);
c.draw();


// Note:- Take Away
// the syntax of ES6 nidykes. so in circle module, we use the export keyword to export one or more objects,
// and then in other modules, we use import to import those objects, from our modules.