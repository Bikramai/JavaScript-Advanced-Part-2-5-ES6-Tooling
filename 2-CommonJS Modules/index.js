const Circle = require('./circle');

// Cohesion:- 
// Things that are higly related, they ot together, this is what we called cohesion in software engineering.


const c = new Circle(10);
c.draw();

// This is how we use commonJS modules in Node.